<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">
    <group android:checkableBehavior="single">
        <item android:id="@+id/home_menu"
            android:icon="@drawable/baseline_home_24"
            android:title="Home"/>
        <item android:id="@+id/profile_menu"
            android:icon="@drawable/baseline_person_24"
            android:title="Profile"/>
        <item android:id="@+id/whatsapp_menu"
            android:icon="@drawable/baseline_whatsapp_24"
            android:title="Contact Us"/>
    </group>
    <item
        android:title="Zona de Riesgo">

        <menu>
            <item android:id="@+id/logout_menu"
                android:icon="@drawable/baseline_logout_24"
                android:title="Salir de la Cuenta"/>
            <item android:id="@+id/delete_menu"
                android:icon="@drawable/baseline_delete_24"
                android:title="Eliminar la cuenta"/>
        </menu>
    </item>
</menu>